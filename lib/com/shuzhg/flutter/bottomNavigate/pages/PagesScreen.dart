import 'package:flutter/material.dart';

class PagaesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pages page'),),
      body: Center(child: Text('Pages page'),),
    );
  }
}
