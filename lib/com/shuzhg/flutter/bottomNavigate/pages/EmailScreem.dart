import 'package:flutter/material.dart';

class EmailScreem extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Email page'),),
      body: Center(child: Text('Email page'),),
    );
  }
}
